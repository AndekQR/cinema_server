package com.app.cinema;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class ReservationTest {


//    private ReservationService reservationService
//
//
//
//
//    @Test
//    @DisplayName("Test Mock helloService + helloRepository")
//    void createReservationTest() throws NotFoundInDB, ChairReservedException {
//        List<Long> chairids=Arrays.asList(5L, 6L);
//        Long movieId = 172L;
//        Long cinemaHallId = 4L;
//        String email = "test@mail.com";
//        BigDecimal price = BigDecimal.valueOf(122.2);
//
//
//
//        Reservation reservation = reservationService.createReservation(email,chairids, movieId, cinemaHallId, price);
//        assertNotNull(reservation);
//        assertNotNull(reservation.getId());
//        assertThrows(ChairReservedException.class, () ->  reservationService.createReservation(email,chairids, movieId, cinemaHallId, price));
//    }

}
